import 'package:flutter/material.dart';

class InfoCardsMediumScreen extends StatelessWidget {
  const InfoCardsMediumScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

