import 'package:flutter/material.dart';

class InfoCardsSmallScreen extends StatelessWidget {
  const InfoCardsSmallScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

